# 诗意时钟 · 项目协作约定

> 本文件随每次会话自动载入,只记**跨会话必须遵守的操作约定**。
> 完整红线 / 闸门见 `README.md`「诚实条款」与 `DECISIONS.md`;数据规则见各 `data/*.json` 的 meta。

## 推送由主编手动执行

- 助手 / 采收**不得自行 `git push`**(与 git-guardrails 钩子一致)。本地 `commit` 可做,**推送只由主编(King)手动执行**。
- **每轮收尾报告必须显式声明推送状态**:`已推送` 或 `待主编推送`。未声明一律视为**待主编推送**。
